
load @cluesurf/bind/code/javascript/native
  find native-bigint
  find native-number
  find native-string

load @cluesurf/bind/code/javascript/es2020/bigint
  find big-int-to-locale-string-options

load @cluesurf/bind/code/javascript/es2020/bigint/big-int
  find big-int

form big-int, name <BigInt>
  link {symbol/to-string-tag}
    text <BigInt>
    work true

  task to-string, name <toString>
    like native-string

    take radix, like maybe, like native-number

  task to-locale-string, name <toLocaleString>
    like native-string

    take locales, like maybe, like native-string
    take options, like maybe, like big-int-to-locale-string-options

  task value-of, name <valueOf>
    like native-bigint

form big-int
  hook self
    link prototype, name <prototype>
      like big-int

    task as-int-n, name <asIntN>
      like native-bigint

      take bits
      take int

    task as-uint-n, name <asUintN>
      like native-bigint

      take bits
      take int
